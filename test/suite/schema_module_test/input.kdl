@module "Modern" {
    @namespace Modern;

    define(*type UInt16) {
        isa = integer;
        size = 4;
        is_signed;
    };

};

@project "Test Project" {
    ` No defined namespace, means it belongs to its parents namespace.

    define(*type UInt16) {
        isa = integer;
        size = 2;
    };

    define(*template NumberPairTemplate) {
        Modern::UInt16 First;
        UInt16 Second;
    };

};
